# 要件定義

## 目的

シミュレーションや計算式の出力のばらつきについて、どの入力がどれだけ寄与しているかを Sobol 感度解析で数値化する。
会社の Windows PC で、エージェントAIを使わずに実行できるようにする。

## 利用者と利用環境

- 利用者: 感度解析を学習中の技術者（Python の基本操作ができる程度）
- 環境: Windows 11（会社PC）、macOS（自宅での開発・確認）
- Python 3.14.5 以上。外部ライブラリは NumPy のみ必須、SciPy は任意

## 機能要件

| ID | 内容 |
|---|---|
| F1 | 入力パラメータを CSV（名前・分布・範囲）で定義できる。分布は一様・正規・三角 |
| F2 | Saltelli サンプリングで入力サンプルを作る。SciPy があれば Sobol 列、無ければ通常の乱数を使う |
| F3 | Python で書いたモデル関数を読み込み、サンプル作成から指数の計算までまとめて実行できる（`run`） |
| F4 | 入力サンプルを CSV に出力し、外部ツールで計算した結果を読み込んで指数を計算できる（`sample` / `analyze`） |
| F5 | 一次効果 S1・総合効果 ST と、ブートストラップによる信頼区間を計算する |
| F6 | N/4・N/2・N での推定値を出し、収束を確認できる |
| F7 | 結果を CSV に保存し、画面に要約（総合効果の大きい順、S1 の合計、注意事項）を表示する |
| F8 | Ishigami 関数の理論値と比べる検算コマンドを持つ（`check`） |
| F9 | 出力が複数あるモデルに対応する |

## 非機能要件

- CSV は UTF-8（BOM の有無を問わない）と Shift_JIS を読み、UTF-8（BOM付き）で書く（Excel で文字化けしない）
- 入力の誤りは、ファイル名・行番号・直し方が分かる日本語のメッセージで知らせる
- インストールしなくても `python run_sobol.py` で実行できる
- 社内データ（計算結果・作業用CSV）はリポジトリにコミットしない

## 対象外（今回は実装しない）

- 2次の交互作用指数 S_ij の個別計算
- 相関のある入力への対応（Shapley 効果など）
- グラフの出力、GUI
- Morris 法など、他の感度解析手法
