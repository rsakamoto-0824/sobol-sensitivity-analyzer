# 設計

## 全体の流れ

```
パラメータCSV → parameters → sampling（行列 A, B, A_B^(i) を作る）
                                 ├─ run:    model_runner でモデル関数を評価 ──────────────┐
                                 └─ sample: samples_file でサンプルCSVを書き出す           │
                                              ↓ 外部ツールで計算して出力を追記               │
                                    analyze: samples_file で出力を読み込む ────────────────┤
                                                                                          ↓
                                  estimators（S1・ST・信頼区間・収束）→ report（CSV保存・画面の要約）
```

## モジュール構成（`src/sobol_sensitivity/`）

| ファイル | 役割 |
|---|---|
| `cli.py` | コマンド（check / run / sample / analyze）の引数処理と、処理の順番の組み立て |
| `parameters.py` | パラメータCSVの読み込み・入力チェック、一様乱数から各分布への変換（逆関数法） |
| `sampling.py` | Saltelli サンプリング。SciPy があれば Sobol 列、無ければ NumPy の乱数 |
| `samples_file.py` | サンプルCSVの書き出しと、出力列（または結果CSV）の読み込み・形式チェック |
| `model_runner.py` | 利用者の .py ファイルからモデル関数を読み込み、全サンプルをまとめて評価 |
| `estimators.py` | 推定式、ブートストラップ信頼区間、収束確認 |
| `report.py` | 結果CSVの保存と、画面に出す要約・お知らせの作成 |
| `benchmark.py` | Ishigami 関数と理論値（検算用） |
| `csv_io.py` | 文字コードを判別するCSV読み込み、書き出し、数値の変換 |
| `errors.py` | 利用者向けの例外 `SobolInputError` |

## サンプルCSVの形式

1行がモデル計算1回分。`block` は行列の種類、`sample_index` は行列内の行番号（1〜N）。

| run_id | block | sample_index | 入力1 | … | 入力d | （出力1 …） |
|---|---|---|---|---|---|---|
| 1 | A | 1 | … | | | |
| N+1 | B | 1 | … | | | |
| 2N+1 | AB_1 | 1 | … | | | |

- 行の並びは A → B → AB_1 → … → AB_d。読み込み時は `block` と `sample_index` で並べ直すので、外部ツールで行順が変わっても計算できる
- 結果を別ファイルにする場合は、`run_id` 列と出力列を持つCSVを `--results` で渡す

## 推定式

- 一次効果: Saltelli et al. (2010) `V_i ≈ mean( f(B) × (f(A_B^i) − f(A)) )`
- 総合効果: Jansen (1999) `V_Ti ≈ mean( (f(A) − f(A_B^i))² ) / 2`
- 計算の前に、出力から全体（全ブロック）の平均を引いて中心化する。一次効果の推定式は出力に一定値を足すと値が変わり、寸法のように平均が大きくばらつきが小さい出力ほど誤差が大きくなるため
- 分母は f(A) と f(B) を合わせた標本分散。中心化も含めて SALib 1.5.2 の `sobol.analyze` と同じ処理で、点推定値が一致することをテストで確認している（SALib が入っている環境のみ）
- 信頼区間: サンプル番号 j を重複ありで選び直すブートストラップで標準偏差を求め、`z × 標準偏差` を半幅とする（SALib と同じ正規近似）
- 収束確認: 先頭 N/4・N/2・N 個のサンプルで推定し直す。Sobol 列は先頭の2のべき乗個でも均一なので、この方法で確認できる

## エラー処理の方針

- 利用者が直せる誤り（CSVの形式、値の範囲、モデル関数の戻り値など）は `SobolInputError` にし、`cli.main` で「エラー: …」と表示して終了コード 2 を返す
- `check` の不合格は終了コード 1
- `sample` は既存ファイルを上書きしない（出力を追記したCSVを誤って消さないため）。上書きは `--overwrite` を付けたときだけ

## 設計上の判断

- **SALib を使わない**: 会社PCでインストールできるか分からないため。推定式は短く、SALib と同じ式を採用して結果を比べられるようにした
- **SciPy は任意**: Sobol 列の生成にだけ使う。無い場合は通常の乱数に切り替え、画面で知らせる
- **Python 3.14.5 以上**: 共通の開発ルールによる。`pyproject.toml` の `requires-python` と、`cli.py` の起動時チェックの両方で確かめる
- **依存ライブラリの下限**: Python 3.14 用の Windows wheel が最初に提供された版（NumPy 2.3.2、SciPy 1.16.1。PyPI で確認）
- **起動スクリプト `run_sobol.py`**: `pip install -e .` ができない環境でも、NumPy さえあれば実行できるようにする
- **サンプル数 N は2のべき乗に限定**: Sobol 列の均一性を保つため。乱数を使う場合も同じ規則にしてそろえた
